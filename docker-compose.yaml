version: '3.2'
services:
  trusted_applet:
    build:
      context: .
      dockerfile: ./release/trusted_applet/Dockerfile
  json_constructor:
    build:
      context: ./release/json_constructor
      dockerfile: ./Dockerfile
